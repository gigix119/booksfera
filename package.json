{
  "name": "booksfera",
  "version": "1.0.0",
  "private": true,
  "description": "Booksfera — premium book analysis (static site)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npx --yes serve dist -p 8080"
  },
  "engines": {
    "node": ">=18"
  }
}
